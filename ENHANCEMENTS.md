ENHANCEMENTS

1. Browser-based push notifications for new matches
    ACTUAL: A toast notification is shown after a user has swiped right on someone who has already swiped them right.
    EXPECTED: The toast notification should retain. However, a push notification should also be sent to the user they made a match with.

2. Browser-based push notifications for new messages
    ACTUAL: No notifications are being sent.
    EXPECTED: The recipient of the message should receive a push notification about the sent message.